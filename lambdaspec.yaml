version: 0.2

env:
  variables:
    S3_BUCKET_IMAGE: ""
    STACK_NAME: ""
    S3_BUCKET_DEPLOY: ""

phases:
  install:
    run-as: root
    runtime-versions:
      python: 3.8
    commands:
      - pip install aws-sam-cli
      - sam --version

  build:
    run-as: root
    commands:
      - cd scripts
      - ./deploy-lambdas.sh
